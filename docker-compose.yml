version: '3.8'

services:
  web:
    build: .
    command: gunicorn grateful_for.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - ./media:/home/app/web/media
      - ./static:/home/app/web/static
    ports:
      - "8000:8000"
    env_file:
      - ./.env